# Supabase (Phase 2 - accounts, progress, achievements). Without these the app
# still runs read-only; accounts/progress are disabled until set.
# Local dev:  supabase start  (prints these)
# Homeberry/cloud: from the project's API settings.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
